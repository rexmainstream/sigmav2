import {weekly_formatted,daily_formatted} from "./timetable_cycle_table"


export default function tt_daily(props) {
    const filler = "This is where the timetable weekly will appear"
    return filler
}